import type {PlaybackHeartbeat,FraudDecision} from "./platform/contracts";
export function assessHeartbeats(events:PlaybackHeartbeat[]):FraudDecision{const reasons:string[]=[];if(events.length<4)reasons.push("insufficient_telemetry");const ordered=[...events].sort((a,b)=>a.at-b.at);let maxGap=0,progress=0,active=0;for(let i=1;i<ordered.length;i++){maxGap=Math.max(maxGap,ordered[i].at-ordered[i-1].at);progress+=Math.max(0,ordered[i].position-ordered[i-1].position)}for(const e of ordered)if(e.playing&&e.visible)active++;if(maxGap>30000)reasons.push("heartbeat_gap");if(progress<50)reasons.push("insufficient_progress");if(active/Math.max(events.length,1)<.75)reasons.push("low_active_ratio");const score=Math.max(0,100-reasons.length*30);return {allow:score>=70,score,reasons}}
