import{describe,it,expect}from"vitest";import{assessHeartbeats}from"./fraud";describe("fraud scorer",()=>{it("accepts coherent active playback",()=>{const id=crypto.randomUUID();const e=Array.from({length:7},(_,i)=>({sessionId:id,trackId:"t",position:i*10,playing:true,visible:true,at:i*10000}));expect(assessHeartbeats(e).allow).toBe(true)});it("rejects weak telemetry",()=>expect(assessHeartbeats([]).allow).toBe(false))})