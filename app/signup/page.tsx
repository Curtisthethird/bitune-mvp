"use client";import {useState} from 'react';import Link from 'next/link';export default function Signup(){const[e,setE]=useState('');async function go(ev:React.FormEvent<HTMLFormElement>){ev.preventDefault();const f=new FormData(ev.currentTarget);const r=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(r.ok)location.href='/dashboard';else setE(d.error)}return <main className="auth"><Link href="/" className="brand">₿ BITUNE</Link><form onSubmit={go}><div className="eyebrow">ARTIST ONBOARDING</div><h1>Create your artist account.</h1><label>Artist name<input name="name" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{e&&<p className="error">{e}</p>}<button>CREATE ACCOUNT</button><p>Already an artist? <Link href="/login">Sign in</Link></p></form></main>}