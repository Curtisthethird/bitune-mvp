import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import Link from 'next/link';import {artistForSession} from '@/lib/store';export default async function Profile(){const c=await cookies(),a=await artistForSession(c.get('bitune_session')?.value);if(!a)redirect('/login');return <main><nav><Link className="brand" href="/dashboard">₿ BITUNE</Link></nav><section className="auth"><div className="eyebrow">ARTIST PROFILE</div><h1>{a.name}</h1><p>{a.email}</p><p>Profile editing, public artist pages and social links are scaffolded for the database-backed beta.</p><Link className="cta" href="/dashboard">BACK TO DASHBOARD</Link></section></main>}
