"use client";import {useState} from 'react';import Link from 'next/link';export default function Login(){const[e,setE]=useState('');async function go(ev:React.FormEvent<HTMLFormElement>){ev.preventDefault();const f=new FormData(ev.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(r.ok)location.href='/dashboard';else setE(d.error)}return <main className="auth"><Link href="/" className="brand">₿ BITTUNE</Link><form onSubmit={go}><div className="eyebrow">ARTIST PORTAL</div><h1>Welcome back.</h1><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{e&&<p className="error">{e}</p>}<button>SIGN IN</button><p>New to BitTune? <Link href="/signup">Create an artist account</Link></p></form></main>}