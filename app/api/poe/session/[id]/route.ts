import {NextResponse} from "next/server";import {getPoeSession} from "@/lib/poe-session";
export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params,s=getPoeSession(id);if(!s)return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json({id:s.id,contentId:s.contentId,listenerId:s.listenerId,verifiedSeconds:Number(s.verifiedSeconds.toFixed(2)),qualified:s.qualified,events:s.events.slice(-20).map((e,i)=>({sequence:e.heartbeat.sequence,position:e.heartbeat.playbackPosition,receivedAt:e.serverReceivedAt,valid:e.result.valid,creditedSeconds:e.result.creditedSeconds,checks:e.result.checks,reasons:e.result.reasons}))})}
