import {NextResponse} from "next/server";import {z} from "zod";const S=z.object({sessionId:z.string().uuid(),trackId:z.string().min(1),position:z.number().min(0).max(86400),playing:z.boolean(),visible:z.boolean()});const buckets=new Map<string,unknown[]>();export async function POST(r:Request){try{const e=S.parse(await r.json()),arr=buckets.get(e.sessionId)??[];if(arr.length>400)return NextResponse.json({error:"Telemetry limit"},{status:429});arr.push({...e,at:Date.now()});buckets.set(e.sessionId,arr);return NextResponse.json({ok:true,count:arr.length})}catch{return NextResponse.json({error:"Invalid heartbeat"},{status:400})}}export function telemetryFor(id:string){return buckets.get(id)??[]}
