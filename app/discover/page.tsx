import Link from 'next/link';import {allTracks} from '@/lib/store';export const dynamic='force-dynamic';export default async function Discover(){const tracks=await allTracks();return <main><nav><Link className="brand" href="/">₿ BITTUNE</Link><Link className="cta" href="/signup">JOIN AS ARTIST</Link></nav><section className="dash"><div className="eyebrow">DISCOVER</div><h1>Independent music on BitTune</h1>{tracks.length===0?<div className="empty">No music published yet. Artist uploads will appear here.</div>:<div className="catalog">{tracks.map(t=><article key={t.id}>{t.artworkUrl?<img src={t.artworkUrl} alt=""/>:<div className="art">₿</div>}<div><h3>{t.title}</h3><p>{t.genre||'Independent'} · {t.verifiedPlays} verified plays</p><Link href={'/listen/'+t.id}>Listen →</Link></div></article>)}</div>}</section></main>}
